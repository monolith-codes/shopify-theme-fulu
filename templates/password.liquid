<div class="customPasswordPage">
    {% form 'storefront_password' %}
       {{ form.errors | default_errors }}
       <input type="password" name="password">
       <button type="submit">Enter</button>
    {% endform %}
</div>

<style>
    .customPasswordPage {
        height: 100vh;
        background-color: green;
    }
</style>